package runamqp

import "testing"

//todo: testme
func TestWorkerPool(t *testing.T) {
	/*
	Given some test handler and a max number of workers..

	- Ensure handler gets called with message
	- Fire off lots of messages and somehow ensure that only max number of workers are workin' at a time
	 */
}